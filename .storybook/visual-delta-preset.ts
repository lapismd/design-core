/**
 * @deprecated Register `@lapismd/storybook-addon-visual-delta` in `.storybook/main.ts`
 * instead. This local preset existed for sibling source development.
 */
export {
  managerHead,
  staticDirs,
  viteFinal,
  webpack,
} from "@lapismd/storybook-addon-visual-delta/preset";
