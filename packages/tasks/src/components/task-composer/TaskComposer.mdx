import {
  Canvas,
  Controls,
  Markdown,
  Meta,
  Primary,
} from "@storybook/addon-docs/blocks";
import * as TaskComposerStories from "./TaskComposer.stories.svelte";
import taskComposerSpec from "../../../specs/components/task-composer.md?raw";

<Meta of={TaskComposerStories} />

# TaskComposer

An inline, low-friction add-task control. Idle state is a quiet trigger
button; active state is a labelled text field with explicit submit and cancel
controls. Implementation status: **Done** for the leaf-control surface.

<Markdown>{taskComposerSpec}</Markdown>

## Usage

<Primary />

## Properties

<Controls />

## Interactions

### Activate the draft field

<Canvas of={TaskComposerStories.Activate} meta={TaskComposerStories} />

### Submit a non-empty draft

<Canvas of={TaskComposerStories.SubmitNonEmpty} meta={TaskComposerStories} />

### Reject an empty submit

<Canvas of={TaskComposerStories.RejectEmpty} meta={TaskComposerStories} />

### Escape cancels only a blank draft

<Canvas of={TaskComposerStories.EscapeBlankCancel} meta={TaskComposerStories} />
