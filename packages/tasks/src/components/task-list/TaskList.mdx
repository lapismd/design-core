import {
  Canvas,
  Controls,
  Markdown,
  Meta,
  Primary,
} from "@storybook/addon-docs/blocks";
import * as TaskListStories from "./TaskList.stories.svelte";
import taskListSpec from "../../../specs/components/task-list.md?raw";

<Meta of={TaskListStories} />

# TaskList

Grouped controlled collection. Implementation status: **Done**.

<Markdown>{taskListSpec}</Markdown>

## Usage

<Primary />

## Properties

<Controls />

## States

<Canvas of={TaskListStories.DefaultGroups} meta={TaskListStories} />
<Canvas of={TaskListStories.CollapseDone} meta={TaskListStories} />
<Canvas of={TaskListStories.Empty} meta={TaskListStories} />
