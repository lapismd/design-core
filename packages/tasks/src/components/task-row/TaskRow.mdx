import { Canvas, Markdown, Meta } from "@storybook/addon-docs/blocks";
import * as TaskRowStories from "./TaskRow.stories.svelte";
import taskRowSpec from "../../../specs/components/task-row.md?raw";

<Meta of={TaskRowStories} />

The canonical implementation contract for this component is rendered below from
`packages/tasks/specs/components/task-row.md`. The TODO interaction stories in
this entry encode completion, selection, explicit detail opening, double-click
parity, keyboard behavior, reorder, and mobile swipe.

<Markdown>{taskRowSpec}</Markdown>

## Implementation placeholder

<Canvas of={TaskRowStories.ImplementationPlaceholder} meta={TaskRowStories} />
