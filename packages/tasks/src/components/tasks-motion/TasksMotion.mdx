import {
  Canvas,
  Controls,
  Markdown,
  Meta,
  Primary,
} from "@storybook/addon-docs/blocks";
import * as TasksMotionStories from "./TasksMotion.stories.svelte";
import motionSpec from "../../../specs/components/tasks-motion.md?raw";

<Meta of={TasksMotionStories} />

# TasksSwipeGesture

Pointer gesture controller for row swipe reveal and pager back. Implementation
status: **Done**.

<Markdown>{motionSpec}</Markdown>

## Usage

<Primary />

## Properties

<Controls />

## States

<Canvas of={TasksMotionStories.SwipeThreshold} meta={TasksMotionStories} />
<Canvas of={TasksMotionStories.ScrollIntentCancels} meta={TasksMotionStories} />
<Canvas of={TasksMotionStories.PagerBackThreshold} meta={TasksMotionStories} />
