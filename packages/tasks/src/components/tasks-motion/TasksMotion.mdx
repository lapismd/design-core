import { Canvas, Markdown, Meta } from "@storybook/addon-docs/blocks";
import * as TasksMotionStories from "./TasksMotion.stories.svelte";
import motionSpec from "../../../specs/components/tasks-motion.md?raw";

<Meta of={TasksMotionStories} />

The canonical implementation contract is rendered below from
`packages/tasks/specs/components/tasks-motion.md`.

<Markdown>{motionSpec}</Markdown>

## Implementation placeholder

<Canvas
  of={TasksMotionStories.ImplementationPlaceholder}
  meta={TasksMotionStories}
/>
