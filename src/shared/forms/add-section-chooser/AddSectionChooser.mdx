import {
  Meta,
  Canvas,
  Controls,
  Primary,
  Source,
} from "@storybook/addon-docs/blocks";
import * as AddSectionChooserStories from "./AddSectionChooser.stories.svelte";
import * as AddSectionChooserVariations from "./AddSectionChooser.variations.stories.svelte";

<Meta of={AddSectionChooserStories} />

# AddSectionChooser

Footer affordance to add a new top-level section: dashed full-width button, then title + type chips.

Add actions belong at the section footer, bottom-aligned — not inline in the header row.

## Guidance

### When to use

- CV/studio forms that append optional section types
- Optional object groups that share the array add flow

### When not to use

- Per-array item add inside `CollapsibleItemList` — use that list’s add button

See also [UI Forms/Guidance](?path=/docs/ui-forms-guidance--docs) and the shared form contract in `FORMS.md`.

## Usage

<Source
  language="html"
  code={
    '<AddSectionChooser\n  open={open}\n  title={title}\n  options={[{ value: "experience", label: "Experience" }]}\n  onOpen={...}\n  onCancel={...}\n  onTitleChange={...}\n  onChoose={...}\n/>'
  }
/>

<Primary />

## Properties

<Controls />

## Examples

### Closed

Dashed add button idle state.

<Canvas
  of={AddSectionChooserVariations.Closed}
  meta={AddSectionChooserVariations}
/>

### Open chooser

Title field + type chips after opening add.

<Canvas
  of={AddSectionChooserVariations.OpenChooser}
  meta={AddSectionChooserVariations}
/>
