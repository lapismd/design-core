import {
  Meta,
  Canvas,
  Controls,
  Primary,
  Source,
  Markdown,
} from "@storybook/addon-docs/blocks";
import * as EmptyStories from "./Empty.stories.svelte";
import * as EmptyVariations from "./Empty.variations.stories.svelte";
import docsBody from "./empty.docs-body.md?raw";

{/* @generated by ui-generator — do not edit */}

<Meta of={EmptyStories} />

# Empty

Use the Empty component to display an empty state.

Adapted from the upstream [shadcn-svelte docs](https://github.com/huntabyte/shadcn-svelte/blob/bf4f461d88526359d0e96e1950f637912bbeebe7/docs/content/components/empty.md).

<Markdown>{docsBody}</Markdown>

## Usage

<Source
  language="tsx"
  code={
    '<script lang="ts">\n  import * as Empty from "@lapismd/design-core/shadcn/empty";\n  import FolderCodeIcon from "@lucide/svelte/icons/folder-code";\n</script>\n\n<Empty.Root>\n  <Empty.Header>\n    <Empty.Media variant="icon">\n      <FolderCodeIcon />\n    </Empty.Media>\n    <Empty.Title>No data</Empty.Title>\n    <Empty.Description>No data found</Empty.Description>\n  </Empty.Header>\n  <Empty.Content>\n    <Button>Add data</Button>\n  </Empty.Content>\n</Empty.Root>'
  }
/>

<Primary />

<Controls />

## Examples

### Preview

<Canvas of={EmptyVariations.Preview} meta={EmptyVariations} />

### Outline

Use the `border` utility class to create an outline empty state.

<Canvas of={EmptyVariations.Outline} meta={EmptyVariations} />

### Background

Use the `bg-*` and `bg-gradient-*` utilities to add a background to the empty state.

<Canvas of={EmptyVariations.Background} meta={EmptyVariations} />
