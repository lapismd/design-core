import { Meta, Canvas, Controls, Primary, Source } from "@storybook/addon-docs/blocks";
import * as CardStories from "./Card.stories.svelte";
import * as CardVariations from "./Card.variations.stories.svelte";

{/* @generated by ui-generator — do not edit */}

<Meta of={CardStories} />

# Card

Displays a card with header, content, and footer.

Adapted from the upstream [shadcn-svelte docs](https://shadcn-svelte.com/docs/components/card.md).

## Usage

<Source language="html" code={"<script lang=\"ts\">\n  import * as Card from \"@stevejuma/ui/shadcn/card\";\n</script>\n\n<Card.Root>\n  <Card.Header>\n    <Card.Title>Card Title</Card.Title>\n    <Card.Description>Card Description</Card.Description>\n  </Card.Header>\n  <Card.Content>\n    <p>Card Content</p>\n  </Card.Content>\n  <Card.Footer>\n    <p>Card Footer</p>\n  </Card.Footer>\n</Card.Root>"} />

<Primary />

<Controls />

## Examples

### Preview

Displays a card with header, content, and footer.

<Canvas of={CardVariations.Preview} meta={CardVariations} />

### Spacing

In addition to the `size` prop, you can use the `--card-spacing` CSS variable to control the spacing between sections and the inset of card parts.

<Canvas of={CardVariations.Spacing} meta={CardVariations} />

### Image

Add an image before the card header to create a card with an image.

<Canvas of={CardVariations.Image} meta={CardVariations} />

