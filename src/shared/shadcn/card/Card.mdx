import {
  Meta,
  Canvas,
  Controls,
  Primary,
  Source,
  Markdown,
} from "@storybook/addon-docs/blocks";
import * as CardStories from "./Card.stories.svelte";
import * as CardVariations from "./Card.variations.stories.svelte";
import docsBody from "./card.docs-body.md?raw";

{/* @generated by ui-generator — do not edit */}

<Meta of={CardStories} />

# Card

Displays a card with header, content, and footer.

Adapted from the upstream [shadcn-svelte docs](https://github.com/huntabyte/shadcn-svelte/blob/bf4f461d88526359d0e96e1950f637912bbeebe7/docs/content/components/card.md).

<Markdown>{docsBody}</Markdown>

## Usage

<Source
  language="html"
  code={
    '<script lang="ts">\n  import * as Card from "@lapismd/design-core/shadcn/card";\n</script>\n\n<Card.Root>\n  <Card.Header>\n    <Card.Title>Card Title</Card.Title>\n    <Card.Description>Card Description</Card.Description>\n  </Card.Header>\n  <Card.Content>\n    <p>Card Content</p>\n  </Card.Content>\n  <Card.Footer>\n    <p>Card Footer</p>\n  </Card.Footer>\n</Card.Root>'
  }
/>

<Primary />

<Controls />

## Examples

### Preview

<Canvas of={CardVariations.Preview} meta={CardVariations} />

### Preview

<Canvas of={CardVariations.Preview2} meta={CardVariations} />

### Spacing

In addition to the `size` prop, you can use the `--card-spacing` CSS variable to control the spacing between sections and the inset of card parts.

<Canvas of={CardVariations.Spacing} meta={CardVariations} />

### Spacing

ComponentPreview name="card-spacing" class="[&_.preview]:h-[34rem]">

Use negative margins with `-mx-(--card-spacing)` to make content go edge to edge while keeping it aligned with the card inset. When the edge-to-edge content sits above a footer, use `-mb-(--card-spacing)` on `CardContent` to remove the section gap.

<Canvas of={CardVariations.Spacing2} meta={CardVariations} />

### Image

Add an image before the card header to create a card with an image.

<Canvas of={CardVariations.Image} meta={CardVariations} />
