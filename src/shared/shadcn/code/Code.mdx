import { Meta, Primary, Controls } from "@storybook/addon-docs/blocks";
import * as CodeStories from "./Code.stories.svelte";

<Meta of={CodeStories} />

# Code

Inline `<code>` for short references (APIs, flags, identifiers) inside prose.

For multi-line highlighted snippets, use **Code Block**. For Lezer-driven form
previews, use **UI Forms / Code Highlighter**.

Import: `@lapismd/design-core/shadcn/code`

Provenance: `spec/records/code-and-code-block.md` (Astryx-inspired, project-authored).

<Primary />

<Controls />
