import { Meta, Canvas, Controls, Primary } from "@storybook/addon-docs/blocks";
import * as ColumnCanvasStories from "./ColumnCanvas.stories.svelte";

<Meta of={ColumnCanvasStories} />

# Column Canvas

Horizontal, selection-driven column cascade for Miller-column / Finder-style
navigation. A typed app-owned controller owns path selection, durable widths,
and collapse/close. `Root` adapts its canvas presentation to its bounded width
without mutating that controller. Compound parts (`Header`, `Toggle`, `Close`,
`Body`, `Item`) provide consistent chrome so hosts do not invent row or header
styling.

This is a project-authored native-CSS family. It is not the same as
[Resizable](?path=/docs/shadcn-layout-resizable--docs): Resizable fills a box
with percentage pane groups, while Column Canvas grows a horizontally scrolling
canvas. Wide layouts use fixed-width columns; compact layouts use a stage-width
active column, previous-column peek, and mandatory snapping.

## Usage

Always mount `Column` parts under `Root`. Configure each column’s `pathLevel`
so cascade visibility is controller-owned (same idea as AppShell self-gating
closed/collapsed chrome). Hosts map `path` keys to domain rows; they do not
`{#if}` columns into the tree. The primary demo uses three columns with
collapse, resize, and a closeable details pane. Use **Show code** on the
preview for a copy-pasteable example.

<Primary />

<Controls />

## Responsive display

`displayMode="auto"` is the default. `Root` observes its own width and resolves
to `wide` at or above `compactBreakpoint` (`960` by default) or `compact` below
it. The resolved state is available as `data-display-mode`.

- **wide** — durable pixel widths, resize handles, trailing spacer, active
  following, and proximity snapping
- **compact** — stage-width expanded columns, a `2.75rem` previous-column peek,
  no durable blank tail or resize handles, mandatory snapping, and guarded
  vertical-wheel handoff at body boundaries
- **fixed** — original fixed geometry and free horizontal scrolling at every
  size, without active following

Responsive widths and scroll position are transient. V1 persistence continues
to contain durable width, collapse, and close state only.

## Examples

### All features

Categories → components → details. Every column is collapsible and resizable;
details is also closeable. Selecting a component reopens details (`openOnSelect`)
and fills it with Name / Id / Role / Import / Category values from that row.

<Canvas of={ColumnCanvasStories.AllFeatures} meta={ColumnCanvasStories} />

### Three-level cascade

Path selection only — categories → components → detail prose, with
toggle-select clearing deeper levels (deeper columns hide via `pathLevel`).

<Canvas of={ColumnCanvasStories.ThreeLevelCascade} meta={ColumnCanvasStories} />

### Closeable columns

`Close` removes a closeable column from the canvas; `controller.open(id)` or
selecting a row restores it when `openOnSelect` is enabled.

<Canvas of={ColumnCanvasStories.CloseableColumns} meta={ColumnCanvasStories} />

### Collapse and expand

`Toggle` collapses a column to a vertical rail; expand restores it.

<Canvas of={ColumnCanvasStories.CollapseAndExpand} meta={ColumnCanvasStories} />

### Resizable columns

Set `resizable: true` on the column config — the handle updates the controller.

<Canvas of={ColumnCanvasStories.ResizableColumns} meta={ColumnCanvasStories} />

### Persisted widths

Inject `createLocalStorageColumnCanvasLayoutPersistence` (or any
`ColumnCanvasLayoutPersistence`) to restore widths and collapse.

<Canvas of={ColumnCanvasStories.PersistedWidths} meta={ColumnCanvasStories} />

### Responsive adaptive canvas

The 700px bounded example resolves to compact, follows the deepest visible
column, and leaves the configured previous-column context visible.

<Canvas
  of={ColumnCanvasStories.ResponsiveAdaptiveCanvas}
  meta={ColumnCanvasStories}
/>

### Fixed compatibility

`displayMode="fixed"` preserves the original fixed-width, free-scroll behavior.

<Canvas
  of={ColumnCanvasStories.FixedCompatibility}
  meta={ColumnCanvasStories}
/>

## Parts

- `Root` — installs the controller, responsive horizontal scroller, and
  wide/fixed trailing spacer
- `Column` — controller-driven width/collapse; optional convenience `title`/`count`
- `Header` / `Title` / `Count` / `HeaderActions` / `Toggle` / `Close`
- `Body` / `Item`
- `createColumnCanvasController` — typed path + layout controller
- `CollapsedColumn` — low-level rail escape hatch (prefer controller collapse)

## Styling

Override `--ui-column-canvas-*` tokens on an ancestor, including
`--ui-column-canvas-compact-peek-width` (default `2.75rem`). Production sources
use native CSS and compose the shared Button for Toggle and Close.
