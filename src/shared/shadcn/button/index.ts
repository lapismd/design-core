import Root, {
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
  BUTTON_VARIANTS,
  BUTTON_SIZES,
} from "./button.svelte";

export {
  Root,
  type ButtonProps as Props,
  Root as Button,
  buttonVariants,
  BUTTON_VARIANTS,
  BUTTON_SIZES,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
};
