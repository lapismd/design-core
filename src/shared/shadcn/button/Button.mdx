import {
  Meta,
  Canvas,
  Controls,
  Primary,
  Source,
  Markdown,
} from "@storybook/addon-docs/blocks";
import * as ButtonStories from "./Button.stories.svelte";
import * as ButtonVariations from "./Button.variations.stories.svelte";
import docsBody from "./button.docs-body.md?raw";

{/* @generated by ui-generator — do not edit */}

<Meta of={ButtonStories} />

# Button

Displays a button or a component that looks like a button.

Adapted from the upstream [shadcn-svelte docs](https://github.com/huntabyte/shadcn-svelte/blob/bf4f461d88526359d0e96e1950f637912bbeebe7/docs/content/components/button.md).

<Markdown>{docsBody}</Markdown>

## Usage

<Source
  language="html"
  code={
    '<script lang="ts">\n  import { Button } from "@lapismd/design-core/shadcn/button";\n</script>\n\n<Button variant="outline">Button</Button>'
  }
/>

<Primary />

<Controls />

## Examples

### Preview

**Updated:** We have updated the button component to add new sizes: `icon-sm` and `icon-lg`. See the
[changelog](/docs/components/button#changelog) for more details. Follow the
instructions to update your project.

<Canvas of={ButtonVariations.Preview} meta={ButtonVariations} />

### Size

<Canvas of={ButtonVariations.Size} meta={ButtonVariations} />

### Default

<Canvas of={ButtonVariations.Default} meta={ButtonVariations} />

### Outline

<Canvas of={ButtonVariations.Outline} meta={ButtonVariations} />

### Secondary

<Canvas of={ButtonVariations.Secondary} meta={ButtonVariations} />

### Ghost

<Canvas of={ButtonVariations.Ghost} meta={ButtonVariations} />

### Destructive

<Canvas of={ButtonVariations.Destructive} meta={ButtonVariations} />

### Link

<Canvas of={ButtonVariations.Link} meta={ButtonVariations} />

### Icon

<Canvas of={ButtonVariations.Icon} meta={ButtonVariations} />

### With Icon

The spacing between the icon and the text is automatically adjusted based on the size of the button. You do not need any margin on the icon.

<Canvas of={ButtonVariations.WithIcon} meta={ButtonVariations} />

### Rounded

Use the `rounded-full` class to make the button rounded.

<Canvas of={ButtonVariations.Rounded} meta={ButtonVariations} />

### Spinner

<Canvas of={ButtonVariations.Spinner} meta={ButtonVariations} />
