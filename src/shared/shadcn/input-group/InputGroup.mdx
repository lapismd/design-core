import {
  Meta,
  Canvas,
  Controls,
  Primary,
  Source,
  Markdown,
} from "@storybook/addon-docs/blocks";
import * as InputGroupStories from "./InputGroup.stories.svelte";
import * as InputGroupVariations from "./InputGroup.variations.stories.svelte";
import docsBody from "./input-group.docs-body.md?raw";

{/* @generated by ui-generator — do not edit */}

<Meta of={InputGroupStories} />

# Input Group

Display additional information or actions to an input or textarea.

Adapted from the upstream [shadcn-svelte docs](https://github.com/huntabyte/shadcn-svelte/blob/bf4f461d88526359d0e96e1950f637912bbeebe7/docs/content/components/input-group.md).

<Markdown>{docsBody}</Markdown>

## Usage

<Source
  language="html"
  code={
    '<script lang="ts">\n  import * as InputGroup from "@lapismd/design-core/shadcn/input-group";\n  import SearchIcon from "@lucide/svelte/icons/search";\n</script>\n\n<InputGroup.Root>\n  <InputGroup.Input placeholder="Search..." />\n  <InputGroup.Addon>\n    <SearchIcon />\n  </InputGroup.Addon>\n  <InputGroup.Addon align="inline-end">\n    <InputGroup.Button>Search</InputGroup.Button>\n  </InputGroup.Addon>\n</InputGroup.Root>'
  }
/>

<Primary />

<Controls />

## Examples

### Preview

<Canvas of={InputGroupVariations.Preview} meta={InputGroupVariations} />

### Icon

<Canvas of={InputGroupVariations.Icon} meta={InputGroupVariations} />

### Text

Display additional text information alongside inputs.

<Canvas of={InputGroupVariations.Text} meta={InputGroupVariations} />

### Tooltip

Add tooltips to provide additional context or help.

<Canvas of={InputGroupVariations.Tooltip} meta={InputGroupVariations} />

### Textarea

Input groups also work with textarea components. Use `block-start` or `block-end` for alignment.

<Canvas of={InputGroupVariations.Textarea} meta={InputGroupVariations} />

### Spinner

Show loading indicators while processing input.

<Canvas of={InputGroupVariations.Spinner} meta={InputGroupVariations} />

### Label

Add labels within input groups to improve accessibility.

<Canvas of={InputGroupVariations.Label} meta={InputGroupVariations} />

### Dropdown

Pair input groups with dropdown menus for complex interactions.

<Canvas of={InputGroupVariations.Dropdown} meta={InputGroupVariations} />

### Custom Input

Add the `data-slot="input-group-control"` attribute to your custom input for automatic behavior and focus state handling.

No style is applied to the custom input. Apply your own styles using the `class` prop.

<Canvas of={InputGroupVariations.CustomInput} meta={InputGroupVariations} />
