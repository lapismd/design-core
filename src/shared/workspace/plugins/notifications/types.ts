export interface NotificationsPluginOptions {
  enabled?: boolean;
  showToasts?: boolean;
  showStatus?: boolean;
}

export interface NotificationsPluginRuntimeOptions {
  showToasts: boolean;
  showStatus: boolean;
}
